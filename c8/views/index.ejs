<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width-device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include ('partials/header', {template: fullName}); %>
    <h3>Име и презиме: <%= ime %> <%= prezime %></h3>
    <ul>
        <% array.forEach(item => { %>
        <li><%= item.imeNaElementot %></li>
        <% }); %>
    </ul>
    <table border="1" width="100%">
        <tr>
            <th>Автор </th>
            <th>Година </th>
        </tr>
        <% books.forEach(book => { %>
            <tr>
                <th> <%= book.author %> </th>
                <th> <%= book.year %> </th>
            </tr>
        <% }); %>
    </table>
</body>
</html>
